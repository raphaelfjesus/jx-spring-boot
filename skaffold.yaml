apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/raphaelfjesus/jx-spring-boot:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/raphaelfjesus/jx-spring-boot:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: jx-spring-boot
        chartPath: charts/jx-spring-boot
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/raphaelfjesus/jx-spring-boot'
          image.tag: '{{.DIGEST_HEX}}'
